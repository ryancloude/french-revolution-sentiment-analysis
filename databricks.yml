bundle:
  name: french-revolution-sentiment-analysis

sync:
  include:
    - src/**
    - notebooks/**
    - data/lookup/**

targets:
  dev:
    default: true
    mode: development
    workspace:
      host: https://dbc-b1968060-7d4e.cloud.databricks.com/
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}