resources:
  jobs:
    gold_figure_stance_by_document:
      name: ${bundle.target}-frc-gold-figure-stance-by-document

      tasks:
        - task_key: build_figure_stance_by_document
          spark_python_task:
            python_file: ../../src/frc_sentiment/gold/build_figure_stance_by_document.py
            parameters:
              - --catalog
              - ${var.catalog}
              - --schema
              - ${var.schema}
          environment_key: default

      environments:
        - environment_key: default
          spec:
            client: "4"