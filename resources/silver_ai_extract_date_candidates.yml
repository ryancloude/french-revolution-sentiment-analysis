resources:
  jobs:
    silver_ai_extract_date_candidates:
      name: ${bundle.target}-frc-silver-ai-extract-date-candidates

      tasks:
        - task_key: build_ai_extract_date_candidates
          spark_python_task:
            python_file: ../src/frc_sentiment/transform/build_ai_extract_date_candidates.py
            parameters:
              - --catalog
              - ${var.catalog}
              - --schema
              - ${var.schema}
          environment_key: default

      environments:
        - environment_key: default
          spec:
            client: "4"